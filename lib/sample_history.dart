import 'package:flutter/material.dart';

import 'main.dart';

class SampleHistory extends StatelessWidget {
  const SampleHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('샘플 신청 현황'),
      ),
    );
  }
}
